#pragma once

int const FieldRows = 8;
int const FieldCols = 8;

enum Color {
    ColInvalid = 0,
    ColBlack,
    ColWhite
};

enum FigureId {
    FigInvalid = 0,
    FigIdEmpty,
    FigIdKing,
    FigIdHorse
    
};
